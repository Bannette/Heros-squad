{{#partial "content"}}

    <p>
        {{#if post.published }}
            <span class="glyphicon glyphicon-star" aria-hidden="true"></span>  Published
        {{else}}
            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Draft
        {{/if}}

        <a href="/posts/{{post.id}}/update">ADD SQUAD</a> <br>
        <a href="/posts/{{post.id}}/delete">DELETE SQUAD(no undo)</a>
    </p>
{{/partial}}

{{> layout.hbs}}
</div>